# Design Notes

Pattern-specific context documents for AI-assisted development. Each file contains implementation guidance, code examples, and architectural decisions for a specific subsystem.

## How These Work

These documents serve as **contextual skills** that AI agents load automatically when working on relevant code:

- **VS Code Copilot**: `.github/copilot-instructions.md` contains a single `<instruction>` entry for this file. The agent loads this index first, then reads the relevant design note(s) from the table below based on the task at hand.

## Maintenance Protocol (Required)

Whenever implementation changes touch any component covered by these design notes, updating the corresponding `*.design.md` file(s) is required as part of the same change.

Update the relevant design note(s) to include:

- Current implementation details and behavior changes
- Relevant code examples (or updated existing examples)
- Design decisions and rationale (the "why")
- Limitations, constraints, or trade-offs explicitly imposed by the user/request

Do not treat design-note updates as optional follow-up documentation; they are part of the implementation definition of done.

When a new component/subsystem is created, create the corresponding design note in `docs/design-notes/` as part of the same implementation change (i.e., automatically within the same work item/PR).

Prefer consistency in changes and implementations: design and implement code so it fits existing architecture, patterns, naming, and stylistic conventions in the codebase. If new files are created, follow established project patterns and style choices unless a documented design decision explicitly requires divergence.

Prefer composition over inheritance in production and test code. Use inheritance only when there is a clear, well-justified is-a relationship and composition would make the design less clear.

Prefer strongly typed approaches over reflection. Avoid unsafe type handling and runtime reflection whenever possible; use explicit contracts, interfaces, and compile-time type checks by default.

## Frontmatter Format

```yaml
---
description: What this skill covers and when to use it
globs:
  - src/SomeFolder/**
  - src/SomeFile.cs
---
```

## Available Skills

| File | Scope | Key Patterns |
|---|---|---|
| [dev-rules.design.md](dev-rules.design.md) | `.github/**`, `.vscode/**` | Terminal command conventions, agent workflow rules (no leading `&`) |
| [state-machine.design.md](state-machine.design.md) | `src/Klikety/NavigatorCoordinator.cs`, `src/Klikety/Navigation/NavigatorStateMachine.cs`, `src/Klikety/Navigation/SessionManager.cs`, `src/Klikety/Navigation/ActionDispatcher.cs`, `src/Klikety/Navigation/DebounceHandler.cs`, `src/Klikety/Grid/LabelGenerator.cs` | Overlay lifecycle, `DeactivateOverlay`, state machine transitions, key scheme, `LabelGenerator`, arrow navigation, coordinator delegation |
| [navigation-modes.design.md](navigation-modes.design.md) | `src/Klikety/Navigation/**`, `src/Klikety/Grid/CrosshairGridCalculator.cs`, `src/Klikety/Grid/LogGridCalculator.cs`, `src/Klikety/Grid/LogScaleGridCalculator.cs`, `src/Klikety/Grid/LogGrid.cs`, `src/Klikety/Config/LogGridKeyPolicy.cs` | `IModeSession`, mode switching, Crosshair SM/renderer, LogCrosshair SM/renderer, LogGrid SM/renderer/calculator, `DynamicKeyReducer`, `ModeConfig` |
| [win32-interop.design.md](win32-interop.design.md) | `src/Klikety/Services/**`, `src/Klikety/Interop/**`, `src/Klikety/Input/**` | Win32 P/Invoke (`RegisterHotKey`, `SetWindowsHookEx`, `SendInput`, `GetMonitorInfo`), keyboard layout independence |
| [grid-rendering.design.md](grid-rendering.design.md) | `src/Klikety/Overlay/**`, `src/Klikety/Config/ThemeLoader.cs` | DIP-space grid, font auto-scaling, external labels, outlined text, theme system |
| [config.design.md](config.design.md) | `src/Klikety/Config/**`, `src/Klikety/App.xaml.cs` | Config loading/validation, `ConfigMigrator`, `FirstRunExtractor`, tray integration, logging |
| [key-press-visualization.design.md](key-press-visualization.design.md) | `src/Klikety/Overlay/KeyPressWindow.*`, `src/Klikety/Services/KeyPressProcessor.cs`, `src/Klikety/Services/KeyPressDisplayManager.cs`, `src/Klikety/Services/MonitorService.cs` | Hook lifecycle (2nd instance), display manager, outlined text HUD, config section, privacy, monitor tracking, activation transaction |
| [testing.design.md](testing.design.md) | `src/Klikety.Tests/**`, `src/Klikety.SmokeTests/**` | Test fakes, testing seams, xUnit patterns, smoke tests |
| [macros.design.md](macros.design.md) | `src/Klikety/Config/MacroModels.cs`, `src/Klikety/Config/MacroStore.cs`, `src/Klikety/Navigation/MacroRecorder.cs`, `src/Klikety/Navigation/MacroPlayer.cs`, `src/Klikety/Navigation/MacroHandler.cs`, `src/Klikety/Overlay/MacroPicker*`, `src/Klikety/Overlay/MacroPlayback*`, `src/Klikety/Overlay/ClickIndicatorWindow.cs`, `src/Klikety/Services/MacroHotKeyService.cs` | Data model, recorder state machine, player engine, picker, click indicator, dispatch priority, macro state mutex |
| [ci-cd.design.md](ci-cd.design.md) | `.github/workflows/release.yml` | Release workflow, two-job pipeline, version policy, security hardening |
| [autopilot-execution.design.md](autopilot-execution.design.md) | `scripts/autopilot/**`, `.devcontainer/autopilot/**`, `.github/agents/autopilot.agent.md`, `.autopilot.json` | Autonomous plan execution, host/container/sandbox modes, auth setup, trust boundaries, agent definition |

## Adding a New Skill

1. Create `<topic>.design.md` in the appropriate subfolder.
2. Add YAML frontmatter with `description` and `globs`
3. Add a row to the Available Skills table below
4. No changes to `.github/copilot-instructions.md` required
